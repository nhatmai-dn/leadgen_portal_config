# Base URL of the portal service. Vite only exposes vars prefixed with VITE_.
VITE_API_URL=https://staging-portal.datanest.vn

# When the portal blocks cross-origin calls, point this at the Vite dev proxy
# configured in vite.config.ts instead:
# VITE_API_URL=/api
